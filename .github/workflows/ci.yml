name: Force Fail Test

on:
  push:
    branches:
      - main

jobs:
  fail-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Print something
        run: echo "Workflow will fail now"

      - name: Fail workflow intentionally
        run: exit 1
